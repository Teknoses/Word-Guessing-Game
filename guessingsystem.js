



function setup() {
  createCanvas(windowWidth, windowHeight);
  background(255);
  class puzzle{
  constructor(phrase,hint,category,difficulty){
    this.phrase = phrase
    this.hint = hint
    this.category = category
    this.difficulty = difficulty
  }
}
  const puzzle = [
     new puzzle('hi','greeting','words','easy'),
    new puzzle('computer','box','technology','medium')
  ]  
  
}

function draw() {
  clear();
}

